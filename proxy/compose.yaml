services:
  caddy:
    build:
      context: ./caddy
    image: ghcr.io/explodinglabs/sku-proxy:0.1.0
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy:/etc/caddy
      - caddy_data:/data
    environment:
      CADDY_SITE_ADDRESS: api.myapp.com

volumes:
  caddy_data:
