#!/bin/sh

docker compose run --rm -it --no-deps \
  --volume ${PWD}/migrations:/repo:rw \
  --volume ${PWD}/scripts:/scripts:ro \
  --volume ${PWD}/caddy/conf:/etc/caddy:ro \
  --env CADDY_AUTO_HTTPS=off \
  iko "$@"
