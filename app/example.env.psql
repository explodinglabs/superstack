-- .env.psql

-- App / database
\set APP_DB myapp

-- Roles (fully-formed identifiers)
\set MIGRATOR_ROLE myapp_migrator

-- Secrets - same as in compose.yaml.
-- Generate with openssl rand -hex 16 |tr -d '\n'
\set MIGRATOR_PASS '''pass'''
\set PGRST_AUTHENTICATOR_PASS '''pass'''
